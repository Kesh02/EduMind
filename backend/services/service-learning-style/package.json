{
    "name": "@edumind/learning-style-service",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8005",
        "start": "uvicorn app.main:app --host 0.0.0.0 --port 8005",
        "test": "pytest tests/ -v",
        "test:cov": "pytest tests/ --cov=app --cov-report=html --cov-report=term",
        "lint": "pylint app/",
        "format": "black app/ tests/",
        "type-check": "mypy app/"
    }
}